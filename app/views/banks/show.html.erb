<%= link_to "Add new bank", new_bank_path %>
<table class="table">
  <tr>
    <td>Bank ID</td>
    <td><%= @bank.code %></td>
  </tr>
  <tr>
    <td>Name</td>
    <td><%= @bank.name %></td>
  </tr>
  <tr>
    <td>Address</td>
    <td><%= @bank.address %></td>
  </tr>
  <tr>
    <%= link_to "Delete", @bank, method: :delete, confirm: "Are you sure?" %>
  </tr>
</table>
<div align="center">_________________________________________________________________________________</div>
<p></p>
<p></p>
<table class="table">
  <tr><td>Branches' ID</td></tr>
  <% @branches.each do |brnch| %>
    <tr><td><%= link_to brnch.branch_no, branch_path(brnch.branch_no) %></td></tr>
  <% end %>
</table>
<%= link_to "Add new bank", new_bank_path %>